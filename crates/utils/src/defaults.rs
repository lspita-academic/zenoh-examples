use std::time::Duration;

pub const DEFAULT_RECV_TIMEOUT: Duration = Duration::from_secs(3);
pub const DEFAULT_LOOP_SLEEP: Duration = Duration::from_secs(1);
pub const DEFAULT_WAIT_SLEEP: Duration = Duration::from_secs(2);
